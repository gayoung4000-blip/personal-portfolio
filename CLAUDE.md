# 김가영 퍼스널 포트폴리오 — 프로젝트 규칙

이 문서는 이 프로젝트의 모든 작업에 항상 적용되는 규칙이다.
코드를 출력하기 전에 이 규칙을 위반하지 않았는지 스스로 점검한다.

## 프로젝트 개요
- UX/UI 디자이너 김가영의 퍼스널 브랜딩 원페이지 포트폴리오 사이트
- 톤: 아이보리 배경 + 차콜 + 골드 포인트, 세리프 대형 타이포 중심의 에디토리얼 스타일
- 주요 섹션: Hero(배경 영상) → About → Journey(회화 전공 → 유치원 미술강사 → EDUCATOR 디바이더 → 교직 → 등산 전환부) → Skill Palette(아치 카드 4개) → Work(품토·일광전구·W:RUN 등) → Contact/Footer
- 작업 환경: 노트북에서 제작하지만, 실제로 열어보는 사람들은 다양한 해상도의 데스크톱 모니터·노트북을 사용한다.
  따라서 "내 화면에서 맞아 보인다"가 기준이 아니라 "모든 화면 폭에서 깨지지 않는다"가 기준이다.
- 배포: Vercel

## 디자인 기준 — 1920px은 "상한 기준점"이지 고정 크기가 아니다
- 첨부되는 Figma 디자인은 폭 1920px 기준으로 그려졌다.
- 이 1920px은 데스크톱 콘텐츠의 최대 폭(max-width)으로 사용한다.
  화면이 1920px보다 넓으면 콘텐츠는 1920px에서 멈추고 중앙 정렬하며, 좌우는 배경색으로 남긴다.
- 화면이 1920px보다 좁으면(노트북 1366~1440, 구형 모니터 1280 등) 콘텐츠는 화면 폭에 맞춰
  비율적으로 줄어든다. 1920px 디자인을 "잘라서" 보여주는 방식은 금지한다.
- 즉 1920px 고정폭 컨테이너를 만들고 넘치는 부분을 숨기거나 가로 스크롤이 생기게 하는 구현은 틀렸다.
  반드시 유동 레이아웃(반응형 단위 + 브레이크포인트)으로 구현한다.

## 반응형 원칙 (모든 코드에 항상 적용)
- 크기 단위: 레이아웃에 px 고정 금지. rem, %, vw, clamp()를 사용한다.
- 대형 타이포는 clamp(최소값, 유동값(vw 기반), 최대값)로 구현한다.
  최대값이 1920px 디자인 기준 크기가 되고, 화면이 좁아지면 자동으로 축소된다.
- 이미지는 max-width: 100%, height: auto를 기본 적용한다.
- 다음 폭에서 모두 검증한다: 2560(대형 모니터) / 1920(디자인 기준) / 1440 / 1366(일반 노트북) /
  1280 / 768(태블릿) / 390(모바일)
  → 2560에서도 레이아웃이 무너지지 않아야 하고(콘텐츠는 1920에서 멈추고 여백만 늘어남),
    1366~1440(노트북 실사용 해상도)에서도 요소가 겹치거나 잘리면 안 된다.
- 768px 이하 모바일:
  - Skill 아치 카드 4개는 호버 대신 탭으로 동작하는 세로 아코디언으로 전환
  - Journey 대형 타이포는 줄바꿈 구조를 유지하며 clamp로 축소
  - 사진 콜라주는 세로 스택으로 재배치
- OS 배율(100~200%)이나 브라우저 줌과 무관하게 레이아웃이 유지되어야 하므로,
  뷰포트 기반 상대 단위와 유동 레이아웃(flexbox/grid)만 사용한다.
  배율은 화면의 "유효 해상도"를 바꿀 뿐이므로, 반응형 브레이크포인트 대응이
  곧 배율 대응이다. 배율을 별도로 처리하는 코드는 필요 없고 만들지 않는다.
- 가로 스크롤이 발생하면 안 된다. 섹션 완성 시마다 overflow-x를 점검한다.
- 노트북(작업 화면, 보통 1366~1440·때로 200% OS 배율)에서 확인했다고 완료로 보지 않는다.
  반드시 브라우저 개발자도구(F12 → Ctrl+Shift+M)로 위 7개 폭을 전부 훑은 뒤 완료 처리한다.

## 전역 스타일 (하드코딩 금지, 변수로만 사용)
- 색상은 CSS 변수로만 사용한다:
  - --ivory: 배경 (정확한 hex는 최초 세팅 시 Figma에서 추출해 정의)
  - --charcoal: 아치 카드, 본문 텍스트
  - --gold: 포인트 요소
- 폰트: 제목/대형 타이포는 세리프, 본문·UI는 산세리프.
  font-family 변수로 정의해 전 섹션에서 재사용한다.
- 타이포 크기는 전역 타입 스케일(변수, clamp 기반)로만 사용한다.
  섹션마다 새로운 폰트 크기 체계를 만들지 않는다.
- 여백은 8px 배수 스케일의 spacing 변수만 사용한다. 큰 여백은 rem 또는 vw 기반으로
  줄어들 수 있게 한다.

## 인터랙션 (GSAP + ScrollTrigger)
- 스크롤 등장 애니메이션 기본값: 아래에서 60px, 0.8s, ease-out,
  섹션이 뷰포트 30% 지점에 닿으면 시작. (별도 지시가 없으면 이 값 사용)
- Skill 아치 카드: 스크롤 시 하단에서 등장, 호버 시 transform-origin: bottom으로
  위를 향해 확대. 텍스트는 하단 정렬 유지. 이동 거리·확대 비율은 vw/rem 기반으로 하여
  화면 폭이 달라져도 카드 간 비례가 깨지지 않게 한다.
- Hero 배경 영상: video 태그에 autoplay, muted, loop, playsinline 필수.
  object-fit: cover, poster 이미지 지정, 위에 반투명 오버레이 + 텍스트.
- prefers-reduced-motion 사용자에게는 애니메이션을 최소화한다.

## 이미지 규칙
- Figma에서 내보낸 에셋은 2x 해상도 기준이다. 표시 크기는 1x(CSS 기준)로 잡는다.
- 사진은 가능한 한 WebP로 변환하고 lazy loading을 적용한다 (Hero 영상 제외).

## 작업 방식
- 섹션 단위로 작업한다. 한 번에 여러 섹션을 만들지 않는다.
- 1차 조립 단계에서는 레이아웃·텍스트·이미지 배치까지만 하고
  인터랙션은 나중에 별도 단계에서 추가한다.
- 기존에 정의된 전역 변수·컴포넌트를 우선 재사용하고, 중복 정의하지 않는다.
- 수정 요청 시 해당 부분만 고치고, 관련 없는 코드를 임의로 리팩토링하지 않는다.
- 섹션 하나를 완성할 때마다 반응형 원칙의 7개 폭 점검을 통과했는지 확인하고 다음으로 넘어간다.

## 금지 사항
- 레이아웃을 인라인 px 고정값으로 잡는 것
- 1920px(또는 그 어떤 값도) 고정폭 컨테이너로 만들고 넘치면 잘라내거나 스크롤바가 생기게 하는 것
- 색상·폰트·여백 하드코딩 (변수 미사용)
- 섹션별로 다른 폰트 크기 체계 생성
- 가로 스크롤 발생
- 요청하지 않은 라이브러리 추가
